import { defineMessages } from 'react-intl'

export const messages = defineMessages({
  cantCreate: {
    id: 'components.Transcription.Editor.cantCreate',
    defaultMessage: 'Surgió un problema al crear el editor'
  }
})
