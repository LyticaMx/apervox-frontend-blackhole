import { defineMessages } from 'react-intl'

export const messages = defineMessages({
  willBeDeleted: {
    id: 'src.components.Dialog.ActionNotification.willBeDeleted',
    defaultMessage:
      'El registro se eliminará definitivamente y quedará registrado para ser auditada esta acción.'
  }
})
