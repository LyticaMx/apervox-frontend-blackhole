import { defineMessages } from 'react-intl'

export const messages = defineMessages({
  badPermissions: {
    id: 'badPermissions',
    defaultMessage: 'No tiene permisos para ver este apartado'
  }
})
