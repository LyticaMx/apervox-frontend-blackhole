import { defineMessages } from 'react-intl'

export const messages = defineMessages({
  title: { id: 'views.Roles.title', defaultMessage: 'Perfiles' },
  nameColumn: {
    id: 'views.Roles.Table.nameColumn',
    defaultMessage: 'Perfíl'
  }
})
